{% extends 'base.html.twig' %}

{% block title %}{{ 'categorie.edit'|trans }}{% endblock %}

{% block body %}
    <div class="d-flex flex-start">
        <a style="text-decoration: none; color: black" href="{{ path('app_produit_index') }}"><i class="fi fi-rr-arrow-small-left" style="font-size: 30px;"></i></a>
        {{ include('categorie/_delete_form.html.twig') }}
    </div>
    <div class="card">
        <div class="card-header">
            <h1 class="text-center">{{ 'categorie.create'|trans }}</h1>
        </div>
        <div class="card-body">
            {{ include('categorie/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
