{% extends 'base.html.twig' %}

{% block title %}{{ 'categorie.list'|trans }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="text-center"> {{ 'categorie.create'|trans }}</h1>
        </div>
        <div class="card-body">
            {{ include('categorie/_form.html.twig') }}
        </div>
    </div>

    <a href="{{ path('app_categorie_index') }}">back to list</a>
{% endblock %}
