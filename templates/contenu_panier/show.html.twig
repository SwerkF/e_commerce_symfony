{% extends 'base.html.twig' %}

{% block title %}{{ 'contenupanier.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'contenupanier.title'|trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'produit.id'|trans }}</th>
                <td>{{ contenu_panier.id }}</td>
            </tr>
            <tr>
                <th>{{ 'produit.quantity'|trans }}</th>
                <td>{{ contenu_panier.quantite }}</td>
            </tr>
            <tr>
                <th>{{ 'produit.date'|trans }}</th>
                <td>{{ contenu_panier.dateAjout ? contenu_panier.dateAjout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contenu_panier_index') }}">back to list</a>

    <a href="{{ path('app_contenu_panier_edit', {'id': contenu_panier.id}) }}">edit</a>

    {{ include('contenu_panier/_delete_form.html.twig') }}
{% endblock %}
